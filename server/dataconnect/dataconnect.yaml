specVersion: "v1beta"
serviceId: "bitwise-backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bitwise-backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
